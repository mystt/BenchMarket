{
  "name": "ai-benchmark-market",
  "private": true,
  "description": "Market-based AI benchmark — Blackjack, Sports, Crops on Hedera",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "start:backend": "cd backend && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:migrate": "cd backend && npm run db:migrate",
    "hcs:create-topic": "cd backend && npx tsx src/hedera/create-topic.ts",
    "hcs:create-inbound-topic": "cd backend && npx tsx src/hedera/create-inbound-topic.ts",
    "hcs:test-inbound": "cd backend && npx tsx scripts/test-inbound-subscription.ts",
    "hcs:ask": "cd backend && npx tsx scripts/ask-benchmark.ts",
    "hcs:test-knowledge-blackjack": "cd backend && npx tsx scripts/test-knowledge-blackjack.ts",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
